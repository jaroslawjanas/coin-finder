# coin-finder configuration example
#
# Copy this file to `.env` and adjust values before running the scanner.

# Ethereum JSON-RPC endpoint that supports batch POST requests.
# Recommended public endpoint (suggested BATCH_SIZE=100): https://ethereum-rpc.publicnode.com
ETH_RPC_URL=https://your-provider.example/v2/your-key

# Optional: label for the provider recorded in CSV output.
# PROVIDER_NAME=ethereum

# Optional: worker configuration overrides.
# WORKERS=8
# BATCH_SIZE=1024
# RPC_TIMEOUT=30
# RPC_MAX_OUTSTANDING=8
# STATS_REFRESH_INTERVAL=0.25

# Optional: directory and filename for hit CSV outputs.
# OUTPUT_DIR=output
# HITS_FILENAME=eth_hits.csv

# Optional: deterministic seed mixed with timestamps (leave unset for more entropy).
# SEED=demo-seed

# Optional: logging level (DEBUG, INFO, WARNING, ERROR).
# LOG_LEVEL=INFO
